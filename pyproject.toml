[project]
name = "workstation"
version = "0.1.0"
description = "Support tooling for the workstation repository"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.2"
]

[tool.uv]
package = false